# OpenAI Configuration
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-5.2
OPENAI_BASE_URL=https://gpt4ifx.icp.infineon.com
OPENAI_CERT_PATH=cert/ca-bundle.crt
OPENAI_MAX_TOKENS=500
OPENAI_TEMPERATURE=0.1

# Database Configuration (optional — for Live Database mode)
DB_HOST=localhost
DB_PORT=3306
DB_USER=eng2sql_user
DB_PASSWORD=changeme
DB_NAME=your_database

# Docker Compose MySQL (only needed when running docker-compose)
MYSQL_ROOT_PASSWORD=rootpassword

# Application Settings
LOG_LEVEL=INFO
STATIC_SCHEMA_PATH=config/database_config.yaml
